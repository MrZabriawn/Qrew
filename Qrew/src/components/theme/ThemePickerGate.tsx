// src/components/theme/ThemePickerGate.tsx
// Unused — theme selection removed. Pass-through only.
import { ReactNode } from 'react';

export function ThemePickerGate({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
