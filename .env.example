# HOI Time Clock / Housing Workforce · Qrew — Environment Variables Template
# Copy to .env.local and fill in values. NEVER commit .env.local to source control.

# ============================================================
# FIREBASE CLIENT SDK (public — safe to expose to browser)
# Firebase Console > Project Settings > Your apps
# ============================================================
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=



# ============================================================
# FIREBASE ADMIN SDK (server-side only — never expose to browser)
# Create a service account in Firebase Console > Project Settings > Service Accounts
# Download the JSON key and extract these three fields.
# ============================================================
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
# Paste the full private key including BEGIN/END markers. In .env.local use literal \n for newlines:
# FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
FIREBASE_ADMIN_PRIVATE_KEY=

# ============================================================
# HOI / HOUSING WORKFORCE CONFIGURATION
# ============================================================
NEXT_PUBLIC_HOI_WORKSPACE_DOMAIN=housingopps.org
NEXT_PUBLIC_ED_EMAIL=director@housingopps.org

# ============================================================
# GOOGLE CALENDAR INTEGRATION
# ============================================================
NEXT_PUBLIC_CALENDAR_ID=

# ============================================================
# QUICKBOOKS ONLINE (QBO) INTEGRATION
#
# Setup steps:
#   1. Sign in at https://developer.intuit.com
#   2. Create an app → select "Accounting" scope
#   3. Copy Client ID and Client Secret from the Keys & Credentials tab
#   4. Add your redirect URI: https://<your-domain>/api/qbo/callback
#      (also add http://localhost:3000/api/qbo/callback for local dev)
#   5. For sandbox testing, use the sandbox credentials (separate from production)
# ============================================================

# Intuit Developer app credentials
QBO_CLIENT_ID=
QBO_CLIENT_SECRET=

# Must match exactly what you registered in the Intuit Developer Portal
QBO_REDIRECT_URI=https://your-domain.web.app/api/qbo/callback

# 'sandbox' for testing, 'production' for real payroll
QBO_ENVIRONMENT=sandbox

# AES-256-GCM encryption key for token storage (32 bytes = 64 hex chars).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# CRITICAL: Rotate this key by re-encrypting all tokens if compromised.
QBO_ENCRYPTION_KEY=

# Random string (≥32 chars) used to HMAC-sign OAuth state parameters (CSRF protection).
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('base64'))"
QBO_OAUTH_STATE_SECRET=

# Firestore organization document ID for this Qrew instance (Housing Opportunities Inc.)
# Create the document manually or via the admin panel: organizations/{orgId}
QBO_ORG_ID=hoi-housing-opportunities

# Payroll timezone for TimeActivity timestamps sent to QBO
QBO_PAYROLL_TIMEZONE=America/New_York

# ============================================================
# NOTES
# ============================================================
# - functions/.env must also contain QBO_* vars for the retry queue Cloud Function
# - Keep functions/service-account-key.json secure and never commit it
# - Separate functions/.env from .env.local — Cloud Functions don't read .env.local
